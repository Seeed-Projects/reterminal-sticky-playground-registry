{
  "name": "reterminal-sticky-playground-registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public integration registry for the reTerminal Sticky Playground.",
  "scripts": {
    "test": "node --test scripts/*.test.mjs",
    "validate": "node scripts/validate-registry.mjs",
    "package:esp-idf": "node scripts/package-esp-idf.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
