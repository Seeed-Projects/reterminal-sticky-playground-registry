# Printable design assets

Add the photo referenced by `preview.image` in `printable.json`.

- Show the printed design fitted on a real reTerminal Sticky.
- Landscape framing works best on the website card (about 4:3).
- Use JPG, PNG, or WebP, up to 5 MB.
