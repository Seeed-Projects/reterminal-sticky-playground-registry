# Template fragments

Template-mode integrations store their plain-text configuration fragments in
this directory.

Each file used by the Playground must be referenced from `template.headerPath`,
`template.footerPath`, or `template.options[].path` in `integration.json`.
